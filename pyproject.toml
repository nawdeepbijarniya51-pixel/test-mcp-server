[project]
name = "mcpservers"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "Navdeep Bijarniya", email = "nawdeepbijarniya51@gmail.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastmcp>=4.0.3",
]

[project.scripts]
mcpservers = "mcpservers:main"

[build-system]
requires = ["uv_build>=0.12.10,<0.13.0"]
build-backend = "uv_build"
